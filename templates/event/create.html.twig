{% extends 'base.html.twig' %}

{% block body %}

    <div>
        <h1>Create an event</h1>
    </div>

    <div>
        {{ form_start(myForm) }}
        {{ form_widget(myForm) }}
        <button>Submit</button>
        {{ form_end(myForm) }}
    </div>

{% endblock %}